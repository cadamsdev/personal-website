---
import Header from '../components/Header.svelte';
import Footer from '../components/Footer.astro';
---

<div class="flex flex-col h-full">
	<Header client:only pathname={Astro.url.pathname} />

	<div class="grid grid-cols-12 flex-grow">
		<slot />
	</div>

	<Footer />
</div>
